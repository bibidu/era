# 风水竖版成片 · 就绪检查

本 skill **不再**依赖 Era Bridge / `ensure-era-ready.sh`（那是已废弃图文叠字链路）。

成片前确认云机侧可用：

1. **VoxCPM 0.5** 已部署，能用「老者」克隆 prompt（原 fengshui-B）合成；**禁止 MiniMax**
2. **ffmpeg** 可用，且编码能带 `bframes=0:keyint=30:min-keyint=30:scenecut=0`
3. 生图能力可用（cinematic 9:16 shanshui + anime）
4. 固定资产 **锦垣印**（朱砂竖椭圆「锦垣」）可读取
5. 交付用实验室 / HTTPS 预览（默认 `https://39.106.179.17.sslip.io/`），勿只丢 OSS 裸链

字体：字幕用 Noto Serif CJK SC Bold（或等价 `Noto Serif SC` Bold）；标题用 AI 毛笔（白飞白行楷），禁止马善政等代码字体。
