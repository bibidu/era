---
description: 当用户要开启抖音「风水号（阳宅篇）」的社媒任务（使用风大师 / 风水号今天发什么 / 风水下一篇）时，必须启用风大师 skill（fengdashi）
alwaysApply: true
---

# 风大师 Skill 强制路由

只要用户提到以下任一意图，**必须**读取并严格执行 `.agents/skills/fengdashi/SKILL.md`（也可用 `/fengdashi` 或 `.cursor/skills/fengdashi`）：

- 使用风大师 / 风大师，开启今天的任务 / 风大师复盘
- 风水号今天发什么 / 风水下一期 / 风水下一篇 / 排一下风水号明天的作品
- 风水号复盘、涨粉、后台数据分析

风大师是**抖音风水号（阳宅篇）**的负责人，和蛇大师（图文号「AI提效实验室」）是**两个账号、两套数据、两套结论**，但推**同一个飞书群**。图文号任务仍走 `shedashi-skill.mdc`。

## 全自动，零确认（硬性）

用户**只负责发布**。一次触发必须跑完：

```
拉数据 → 复盘 → 定选题/标题/档期 → 写正文 → 出诗意背景 → 出内容页 → 上传 OSS → 入库 → 飞书通知
```

- **禁止**向用户确认选题、标题、正文、版面
- **禁止**发标题候选列表、高亮设置页、标题排版设置页
- **禁止**写完文案就停下等确认
- 交付＝飞书机器人推「风水号下一篇已就绪」+ 对话里只发 HTTPS `https://39.106.179.17.sslip.io/?tab=data`

## 异常也必须推飞书（硬性）

自己修不好、或需用户拍板时，除对话说明外**必须**推红色告警卡：

```bash
node scripts/fengdashi-notify.mjs --alert --stage "…" --detail "<报错原文>" --action "<需要用户做什么>"
```

判定标准：**这一轮没能推出「已就绪」卡，就一定要推一张告警卡**，不允许两张都没有。
`fengdashi-publish.mjs` 内部已自动兜底；手工步骤（Era 起不来、出图失败、读不到库）要自己调。

## 关键约束

- **档期用脚本的 `nextSlot`，禁止自己拍**：`node scripts/fengdashi-analyze.mjs`。抖音断更惩罚要求相邻两篇间隔 **≤ 2 天**（风水号只发风水，只按风水已发布记录算）；只投早 07:40–08:00。
- **可回收分析的必要条件**：`work_type === '风水'` **且** `extract_status === '提取成功'`，缺一不可；用 `isFengAnalyzable()`。
- **内容生成走风水 skill**（`.agents/skills/fengshui/SKILL.md`）的改写方向与排版，但**跳过其中所有确认步**。
- **多行一级标题用多行 `#`，不要调 `era_set_title`**（会把封面标题串字）；`title` 由 `fengdashi-publish.mjs` 入库时写。
- 名称 / 头像 / 背景图 / 简介 / 定位**永不改动**。
- 结论依据见 `.agents/skills/fengdashi/references/playbook.md`；跑完把新结论写回去。
