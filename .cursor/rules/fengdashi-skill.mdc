---
description: 当用户要开启抖音「风水号（阳宅篇）」的社媒任务（使用风大师 / 风水号今天发什么 / 风水下一篇）时，必须启用风大师 skill（fengdashi）
alwaysApply: true
---

# 风大师 Skill 强制路由

只要用户提到以下任一意图，**必须**读取并严格执行 `.agents/skills/fengdashi/SKILL.md`（也可用 `/fengdashi` 或 `.cursor/skills/fengdashi`）：

- 使用风大师 / 风大师，开启今天的任务 / 风大师复盘
- 风水号今天发什么 / 风水下一期 / 风水下一篇 / 排一下风水号明天的作品
- 风水号复盘、涨粉、后台数据分析

风大师是**抖音风水号（阳宅篇）**的负责人，和蛇大师（「AI提效实验室」）是**两个账号、两套数据、两套结论**，但推**同一个飞书群**。实验室号任务仍走 `shedashi-skill.mdc`。

## 决策全自动，媒体制作强制确认（硬性）

```
拉数据 → 复盘 → 定选题/标题/档期 →（有抖音/视频链接则）风水竖版成片 → 飞书通知
```

- **禁止** Era 叠字、gc-minimal 底图、4–6 页分篇出图、`image_previews` 多页入库
- **成片**一律走 `.agents/skills/fengshui/SKILL.md`（风水竖版成片）：按 CosyVoice > VoxCPM2 > VoxCPM 0.5 选择引擎并执行音频门禁
- **禁止**向用户确认选题、标题、正文；但必须让用户确认通过门禁的 **10 秒真实克隆音频 + 最终首帧**，确认前不得继续全片或推“已就绪”
- 交付＝飞书「风水号下一篇已就绪」+ 对话里只发 HTTPS `https://39.106.179.17.sslip.io/?tab=data`（或成片预览链）

## 异常也必须推飞书（硬性）

```bash
node scripts/fengdashi-notify.mjs --alert --stage "…" --detail "<报错原文>" --action "<需要用户做什么>"
```

判定标准：**这一轮没能推出「已就绪」卡，就一定要推一张告警卡**。
`fengdashi-publish.mjs` 若仍被用于历史图文入库，**不要**再当现行出图主路径；成片交付以 fengshui 视频 skill 为准。

## 关键约束

- **档期用脚本的 `nextSlot`，禁止自己拍**：`node scripts/fengdashi-analyze.mjs`。断更间隔 **≤ 2 天**（只按风水已发布记录）；只投早 07:40–08:00
- **可回收分析**：`work_type === '风水'` **且** `extract_status === '提取成功'`；用 `isFengAnalyzable()`
- 名称 / 头像 / 背景图 / 简介 / 定位**永不改动**
- 结论依据见 `.agents/skills/fengdashi/references/playbook.md`；跑完把新结论写回去
