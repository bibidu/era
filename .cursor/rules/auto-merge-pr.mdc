---
description: 改动完成后的 PR 直接合入 main，无需询问用户确认
alwaysApply: true
---

# PR 自动合入主分支

本仓库约定：Cloud Agent / 本机 Agent 完成功能改动并创建 PR 后，**直接将 PR 合入 `main`**，不要等待或询问用户是否合并。

## 必须

1. 实现、自测通过后：commit → push → 创建/更新 PR
2. 随即把该改动分支 **merge 进 `main` 并 push**（或等价合入），无需二次确认
3. 合入后可关闭对应 PR（若 GitHub 未自动关闭）

## 例外

仅当用户**明确说不要合入 / 先别合并 / 保持 draft 等审**时，才跳过合入。
