---
description: 改动完成后的 PR 直接合入 main，无需询问；合入后部署自建站并回传固定 URL
alwaysApply: true
---

# PR 自动合入主分支 + 回传最新 URL

本仓库约定：Cloud Agent / 本机 Agent 完成功能改动并创建 PR 后，**直接将 PR 合入 `main`**，不要等待或询问用户是否合并；合入推送后还必须把**最新线上预览 URL**发给用户。

## 必须

1. 实现、自测通过后：commit → push → 创建/更新 PR
2. 随即把该改动分支 **merge 进 `main` 并 push**（或等价合入），无需二次确认
3. 合入后可关闭对应 PR（若 GitHub 未自动关闭）
4. **执行** `npm run deploy:swas`（先确保 `origin/main` 已含改动；脚本 SSH 到服务器 `git pull` + 构建并同步 `/opt/era-web`）。详见 skill **swas-deploy** / `deploy/swas/server.env`
5. 在对话里**直接发给用户**固定站点 URL：`http://39.106.179.17/`（可按需带 `?tab=data` / `highlight` / `title` / `stitch`）
6. 禁止只发 Actions 页面链接代替预览 URL

## 例外

仅当用户**明确说不要合入 / 先别合并 / 保持 draft 等审**时，才跳过合入与回传。
