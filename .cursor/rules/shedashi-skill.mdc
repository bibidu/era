---
description: 当用户要开启抖音账号「AI提效实验室」的社媒任务（使用蛇大师 / 今天发什么 / 下一期）时，必须启用蛇大师 skill（shedashi）
alwaysApply: true
---

# 蛇大师 Skill 强制路由

只要用户提到以下任一意图，**必须**读取并严格执行 `.agents/skills/shedashi/SKILL.md`（也可用 `/shedashi` 或 `.cursor/skills/shedashi`）：

- 使用蛇大师 / 蛇大师，开启今天的任务 / 蛇大师复盘
- 今天发什么 / 下一期发什么 / 下一篇 / 排一下明天的作品
- 抖音账号复盘、涨粉、后台数据分析、社媒负责人

## 全自动，零确认（硬性）

蛇大师是账号的抖音负责人，用户**只负责发布**。一次触发必须跑完：

```
拉数据 → 复盘 → 定选题/标题/档期 → 写正文 → 出封面 → 出内容页 → 上传 OSS → 入库 → 飞书通知
```

- **禁止**向用户确认选题、标题、正文、封面、版面
- **禁止**发标题候选列表、高亮设置页、标题排版设置页
- **禁止**写完文案就停下等确认
- 交付＝飞书机器人推「第 N 期已就绪」+ 对话里只发 HTTPS `https://39.106.179.17.sslip.io/?tab=data`

## 异常也必须推飞书（硬性）

自己修不好、或需要用户拍板时，除了在对话里说明，**必须**推红色告警卡：

```bash
node scripts/shedashi-notify.mjs --alert --stage "…" --detail "<报错原文>" --action "<需要用户做什么>" --issue N
```

判定标准：**这一轮没能推出「已就绪」卡，就一定要推一张告警卡**，不允许两张都没有。
`shedashi-publish.mjs` 内部已自动兜底；手工步骤（Era 起不来、出图失败、读不到库）要自己调。

## 关键约束

- 期号由 `node scripts/shedashi-analyze.mjs` 给出（读 `outline` 前缀），禁止手编
- **档期用脚本的 `nextSlot`，禁止自己拍**：抖音断更惩罚要求相邻两篇间隔 **≤ 2 天**（账号级，风水/健身也占位）；在此之上走固定周节律 **周一/周二/周三/周四/周六 早 07:40–08:00**（周五实测差，周日无样本）
- **可回收分析的必要条件**：`work_type === '图文'` **且** `extract_status === '提取成功'`，两个缺一不可；用 `isAnalyzable()`，别另写过滤
- 名称 / 背景图 / 简介 / 定位**永不改动**
- 结论依据见 `.agents/skills/shedashi/references/playbook.md`；跑完要把新结论写回去
- 图文出图沿用 `tuwen` skill 的技术要点，但**跳过其中所有确认步**
