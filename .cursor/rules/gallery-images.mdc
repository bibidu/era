---
name: gallery-images
description: >-
  当 Agent 需要向用户展示或发送图文图片时，自动整理到仓库 gallery，
  提供批量下载页面并清理过期图片。
alwaysApply: true
---

# Gallery 图片管理规则

## 触发时机

每次向用户输出图片（Era 导出、截图、生成图等）时，必须执行以下完整流程。

## 流程

### 1. 确定今日目录

图片统一放到 `public/gallery/<YYYY-MM-DD>/`，日期使用 UTC+8（中国时间）。

```bash
TODAY=$(TZ='Asia/Shanghai' date +%Y-%m-%d)
GALLERY_DIR="public/gallery/${TODAY}"
mkdir -p "$GALLERY_DIR"
```

### 2. 清理超过 1 天的旧图片目录

在新增图片 **之前**，删除 `public/gallery/` 下所有日期早于今天的子目录（仅删除 YYYY-MM-DD 格式的目录，保留 index.html 等文件）：

```bash
TODAY=$(TZ='Asia/Shanghai' date +%Y-%m-%d)
for dir in public/gallery/*/; do
  dirname=$(basename "$dir")
  # 只处理 YYYY-MM-DD 格式目录
  if [[ "$dirname" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]] && [[ "$dirname" < "$TODAY" ]]; then
    rm -rf "$dir"
    echo "已删除旧目录: $dir"
  fi
done
```

### 3. 复制图片到今日目录

将本次生成的图片（PNG/JPG）复制到 `public/gallery/<TODAY>/`。
文件名保持原样（如 `graphic-page-01.png`）。

### 4. 更新 gallery/index.html

从 `public/gallery/` 扫描所有日期子目录及其图片，用下方模板重新生成 `public/gallery/index.html`。

**模板要点（详见下方完整模板）：**
- 深色风水质感风格
- 按日期分组展示（最新在前）
- 支持灯箱预览（点击放大、左右切换、键盘）
- **iOS 设备**：检测到 iOS 时隐藏下载按钮，改为「长按图片保存到相册」提示，并对每张图使用 Web Share API（`navigator.share`）分享按钮
- **非 iOS 设备**：保留 `<a download>` 单张下载 + 批量下载按钮
- 批量下载在 iOS 上改为逐张调用 `navigator.share`

### 5. 提交并推送到 gh-pages（立即生效）

```bash
# 使用 git worktree 操作 gh-pages 分支
git fetch origin gh-pages
git worktree add /tmp/gh-pages-gallery origin/gh-pages 2>/dev/null || true
mkdir -p /tmp/gh-pages-gallery/gallery/${TODAY}
cp public/gallery/${TODAY}/*.png /tmp/gh-pages-gallery/gallery/${TODAY}/ 2>/dev/null || true
cp public/gallery/${TODAY}/*.jpg /tmp/gh-pages-gallery/gallery/${TODAY}/ 2>/dev/null || true
cp public/gallery/index.html /tmp/gh-pages-gallery/gallery/index.html

# 删除 gh-pages 中的旧日期目录
for dir in /tmp/gh-pages-gallery/gallery/*/; do
  dirname=$(basename "$dir")
  if [[ "$dirname" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]] && [[ "$dirname" < "$TODAY" ]]; then
    rm -rf "$dir"
  fi
done

cd /tmp/gh-pages-gallery
git add gallery/
git commit -m "gallery: 更新 ${TODAY} 图片"
git push origin HEAD:gh-pages
cd -
```

### 6. 同步到功能分支（PR 持久化）

将 `public/gallery/` 的变更提交到当前功能分支或新建 `cursor/gallery-update-<date>-f1ab` 分支，并更新/创建 PR。

### 7. 向用户返回链接

输出格式：

```
图片已上传，下载页：
https://bibidu.github.io/era/gallery/
```

---

## iOS 兼容说明

iOS Safari 不支持 `<a download>` 触发文件下载，图片须通过以下方式处理：

- **优先**：使用 `navigator.share({ files: [file] })` — iOS 15+ 支持，可直接存入相册
- **降级**：在新标签页打开图片（`window.open(src, '_blank')`），提示用户长按保存
- 检测方式：`/iPhone|iPad|iPod/i.test(navigator.userAgent)`

---

## 注意事项

- `public/gallery/index.html` 是所有批次共用的入口，每次更新时重新生成完整文件
- 图片文件较大（2-3MB/张），注意 git 提交体积
- `gh-pages` 分支是 force-orphan 部署，直接推送的内容在下次 main 合并部署时会被覆盖；PR 合并到 main 后才能持久保留
