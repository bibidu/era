---
description: 当用户要用 Era / 图文 / 封面做小红书/抖音出图时，必须启用图文 skill（tuwen）
alwaysApply: true
---

# 图文 Skill 强制路由

只要用户提到以下任一意图，**必须**读取并严格执行项目 skill `.agents/skills/tuwen/SKILL.md`（也可用 `/tuwen` 或 `.cursor/skills/tuwen`）：

- 用 era / 图文skill 生成图文
- 根据标题/大纲出小红书或抖音图
- 图文高亮、分页导出、发图
- 封面skill / 生成封面 / 社媒封面 / cover skill（封面已内联进图文 skill）

**风水 / 阳宅 / 风水风格** → 改走 `.agents/skills/fengshui/SKILL.md`（见 `fengshui-skill.mdc`），不要用本路由。

不要跳过确认步骤；不要在未启动 Era 服务时假装已出图。

默认流程：先问大纲 → 生成内容并确认 → 确认标题＋封面字段 → 内联封面步骤出封面 → 用户自设高亮 → 封面＋内容高亮页拼合确认。内容图**不含一级标题**；默认导出**抖音 9:16**。

高亮：`highlightSetupPagesUrl(shareId)`（`http://39.106.179.17/?tab=highlight&shareId=…`）；禁止对整段 query 二次 `encodeURIComponent`。
确认与最终交付均默认入库后只发线上预览链接（建议 `?tab=data`）；禁止对话框直发 OSS 图链；OSS 临时 HTML 仅用户强烈要求时。见 `preview-link-only.mdc`、`image-preview-delivery.mdc`。
高亮色板不含灰色。
