---
description: 当用户要用 Era 做图文/小红书/抖音出图时，必须启用 era skill
alwaysApply: true
---

# Era Skill 强制路由

只要用户提到以下任一意图，**必须**读取并严格执行项目 skill `.cursor/skills/era/SKILL.md`（也可用 `/era`）：

- 用 era 生成图文
- 根据标题/大纲出小红书或抖音图
- 图文高亮、分页导出、发图

不要跳过确认步骤；不要在未启动 Era 服务时假装已出图。

流程分支（见 skill）：
- **风水**：封面标题与内容同在一套 Era 图里（原流程）。
- **非风水（默认）**：先问大纲 → 生成内容并确认 → 确认标题＋封面所需信息（字段见「封面 skill」）→ 按封面 skill 出封面图 → 用户自设高亮 → 封面＋内容高亮页拼成横版总览确认 → 逐张发图。内容图**不含一级标题**，只含二级标题/正文/图片/代码块等；需要配图时在预览环节混排展示。

高亮步骤：创建 Supabase share 后发送 GitHub Pages URL（`shareId`），引导用户复制配置发回。
导出后必须先发拼图总览供确认，用户确认后再发各页独立图片。
出图后必须用 `era_create_export_share` 把最终图上传 GitHub Pages，并把预览/下载页 URL（`?exportShare=1&shareId=...`）发给用户，供其在线预览并下载原图；不要只发聊天图或本机路径。
