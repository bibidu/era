# 图文交付：只发线上预览链接

用户约定：图文 / 风水 / 社媒出图任务在对话框里**只发线上预览链接**，不要发各页独立图片，也不要为确认来回贴图。

## 必须

1. 导出图须上传并**按序写入**业务库 `image_previews`（及 `cover_url`）；[0]=封面（`__cover_keep__` 永久链）
2. 对用户最终回复：**只给自建站 HTTPS 预览 URL**（`https://39.106.179.17.sslip.io/`；社媒任务建议带 `?tab=data`）。勿发 HTTP 裸 IP（Safari 无法「保存到相册」）
3. 确认阶段默认也不贴图、不发 OSS 图链；与最终交付一样走入库 + 自建站
4. OSS 临时 HTML 仅用户**强烈要求**时可用

## 禁止

- 在对话里逐张粘贴各页 PNG 的 OSS / Supabase 签名 URL 或永久图链
- 用 Gallery / `/gallery/` 链接代替预览 URL
- 用非社媒 HTML 预览页代替社媒入库（图文 / 风水必须走业务库 + 自建站，除非用户强烈要求 HTML）
