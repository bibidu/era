# 图文交付：只发线上预览链接

用户约定：图文 / 社媒出图任务在对话框里**只发线上预览链接**，不要发各页独立图片，也不要为确认来回贴图。

## 必须

1. 导出图须上传并**按序写入**业务库 `image_previews`（及 `cover_url`）；[0]=封面（`__cover_keep__` 永久链）
2. 对用户最终回复：**只给自建站预览 URL**（`http://39.106.179.17/`；社媒任务建议带 `?tab=data`）
3. 确认阶段默认不贴图、不发 OSS 图链

## 禁止

- 在对话里逐张粘贴各页 PNG 的 OSS / Supabase 签名 URL 或永久图链
- 用 Gallery / `/gallery/` 链接代替预览 URL
- 用非社媒 HTML 预览页代替社媒入库（社媒必须走业务库 + 自建站）

## 非社媒例外

非社媒生成图（风水临时确认、封面试稿等）走 HTML 预览页，见 `.cursor/rules/image-preview-delivery.mdc`。
