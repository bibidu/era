# 图文交付：只发线上预览链接

用户约定（2026-07-29）：图文 / 社媒出图任务在对话框里**只发线上预览链接**，不要发各页独立图片。

## 必须

1. 导出图仍须上传并写入 Supabase 等业务字段（如 `image_previews`）
2. 对用户最终回复：**只给 EdgeOne 完整预览 URL**（须含 `eo_token` / `eo_time`）
3. 确认阶段最多发一张拼图总览；不要附带 graphic-page-01…N 链接

## 禁止

- 在对话里逐张粘贴各页 PNG 的 OSS / Supabase 签名 URL
- 用 Gallery / `/gallery/` 链接代替预览 URL

## 例外

- **封面 skill** 单张封面：仍可直发一张封面签名 URL
- 用户**明确要求**「发各页原图 / 逐张发图」时，才发分图
