---
description: 标题排版设置页（Title Composer）何时提供给用户
alwaysApply: true
---

# 标题排版设置页

用户需要精细控制标题（换行、拉伸、字号、间距、字体、行内色）时，**主动提供标题设置页**，不要只口头描述。

## 入口

- 顶栏 Tab「标题」，或 URL：`?tab=title&text=<当前帖子标题>`
- 发给用户时用固定前端：`http://39.106.179.17/?tab=title&text=…`
- **必须注入当前帖子标题**：用 `text`（或别名 `title`）参数传入用户已确认的社媒标题；多行可用换行（URL 编码 `%0A` 或字面 `\n`）。**禁止**只发裸 `?tab=title`（会落回固定 demo「西北绝不能…」）
- 可用：`titleComposerPagesUrl(当前标题)`（`src/agent/supabaseHighlightSetup.ts`）合并参数；**禁止**对整段 query 再 `encodeURIComponent`（聊天客户端易二次编码）

## 流程约定

1. 用户在设置页调好后，点「一键复制配置参数」把配置发回
2. Agent 用 `node scripts/generate-title-composer.mjs --full --input <json> --out <png>` 按配置出完整 9:16 图
3. 出图须加载阿里妈妈数黑体；上传 OSS 后对话框直发签名 URL

## 布局

预览与最终图共用 `getGraphicLayout`；配置在底部可拖高弹簧层。
